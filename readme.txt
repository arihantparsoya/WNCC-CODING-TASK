The task in implemented using Python3

Assumptions:

* Only distance by 'legs'(or auto) is take and other means of transports are ignored

Some comments:

*File can be passed to the program in the terminal itself.
  syntax: python wncc.py <file_name>

*Haversine method is used to find the distance between 2 points in the globe

Modules Used:

* math
* json
* requests
* copy
